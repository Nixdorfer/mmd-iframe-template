{
	"name": "game-engine-v4",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "pnpm --filter @engine/editor dev",
		"build": "pnpm -r build",
		"build:wasm": "cd crates/engine-wasm && wasm-pack build --target web --out-dir ../../packages/common/wasm"
	},
	"devDependencies": {
		"typescript": "^5.3.3",
		"vite": "^5.0.0"
	}
}
